export interface User {
    name: string
    email: string
    avatar: string
}

// Este sería reemplazado por datos reales del usuario autenticado
export const userData: User = {
    name: "anderson",
    email: "anderson.almeyda@dinet.com.pe",
    avatar: "/avatars/shadcn.jpg",
}